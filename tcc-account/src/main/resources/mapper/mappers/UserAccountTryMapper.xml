<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuandai.architecture.repository.UserAccountTryRepository">
    <resultMap id="baseResultMap" type="com.tuandai.architecture.domain.UserAccountTry">
        <result column="uid" property="uid" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="account" property="account" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" javaType="java.sql.Date" jdbcType="TIMESTAMP"/>
    </resultMap>

    <update id="createIfNotExistsTable">
        CREATE TABLE IF NOT EXISTS t_user_account_try (
         uid  VARCHAR(100), 
         name VARCHAR(1024),
         account INT,
         create_time TIMESTAMP,
		 PRIMARY KEY (uid));
    </update>

    <update id="truncateTable">
        TRUNCATE TABLE t_user_account_try;
    </update>
    
    <update id="dropTable">
        DROP TABLE IF EXISTS t_user_account_try;
    </update>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="uid">
        INSERT INTO t_user_account_try (
          uid, name, account, create_time
        )
        VALUES (
        #{uid,jdbcType=VARCHAR},
        #{name,jdbcType=VARCHAR},
        #{account,jdbcType=INTEGER},
        #{createTime,jdbcType=TIMESTAMP}
        )
    </insert>
    
    <delete id="delete">
        DELETE FROM t_user_account_try WHERE uid = #{uid,jdbcType=VARCHAR}
    </delete>
    
    
    <select id="getById" resultMap="baseResultMap">
        SELECT  uid, name, account, create_time 
        FROM t_user_account_try WHERE uid = #{uid,jdbcType=VARCHAR}
    </select>
</mapper>
